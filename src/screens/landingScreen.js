import React from 'react';
import {Text, View} from 'react-native';

import Landing from '_templates/landing';

const LandingScreen = props => {
  return <Landing {...props} />;
};

export default LandingScreen;
