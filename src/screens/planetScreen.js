import React from 'react';
import {Text, View} from 'react-native';

import Planet from '_templates/planet';

const PlanetScreen = props => {
  return <Planet {...props} />;
};

export default PlanetScreen;
