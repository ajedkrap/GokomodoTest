import React from 'react';
import {Text, View} from 'react-native';

import Splash from '_templates/splash';

const SplashScreen = props => {
  return <Splash {...props} />;
};

export default SplashScreen;
